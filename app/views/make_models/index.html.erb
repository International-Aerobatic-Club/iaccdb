<h1>Makes and Models</h1>

<p>A thumbs-up "👍" next to a model name indicates
that a curator has confirmed the make and model combination.
</p>

<dl class='make-models'>
  <% @make_models.each_key do |make| %>
    <dt class="make"><%= make_or_unspecified(make) %></dt>
    <dd class="make">
      <ul class="models">
        <%= render @make_models[make] %>
      </ul>
    <dd>
  <% end %>
</dl>
