<p id="notice"><%= notice %></p>

<h1><%= link_to @pilot.name, pilot_path(@pilot) %></h1>
<h2><%= link_to (@contest) do "#{@contest.name} #{@contest.start}" end %></h2>

<% @flight_scores.each do |f,s| %>
  <div class="flightScores">
  <h3><%= f.flight.displayName %></h3>
  <table class="scores">
  <thead><tr>
  <th>F</th>
  <% (1 .. s[0].count).each do |j| %>
    <th>J<%= j.to_s %></th>
  <% end %>
  </tr></thead>
  <tbody>
  <% pi = s.count - 1 %>
  <% (0 ... pi).each do |f| #figures %>
    <tr>
    <td><%= f.to_s %></td>
    <% s[f].each do |js| #judges %>
      <td><%= js.to_s %></td>
    <% end #judges %>
    </tr>
  <% end #figures %>
  <tr>
  <td>P</td>
  <% s[pi].each do |js| #judges %>
    <td><%= js.to_s %></td>
  <% end #judges %>
  </tr>
  </tbody>
  </table>
  </div>
<% end #flights %>
