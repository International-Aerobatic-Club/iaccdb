rake db:migrate; rake db:test:prepare

decide algorithms for judge metrics
Spearman's rank correlation coefficient
http://mathworld.wolfram.com/SpearmanRankCorrelationCoefficient.html
CIVA RI

'rspec spec' runs all of the tests.  
End test file names in spec directory with _spec.rb

Display results and rankings

app/views/contests/show.html.erb
app/controllers/contests_controller.rb

implement and test:
    cur_jf_results = IAC::RankComputer.computeJudgeMetrics(flight)
    app/models/f_result.rb
    app/models/jf_result.rb

Then display the metrics

Then jc_results rollup for category
  and display those
