rake db:migrate; rake db:test:prepare

'rspec spec' runs all of the tests.  
End test file names in spec directory with _spec.rb

Parsed penalties from manny get stored times 10.  No need to do that.

extract yaml files using the scraper on my machine
read yaml files into the database on the server
the server doesn't run nokogiri, probably because of libxml problems

